# DeepSeek API 接口调用说明

## 1. 基础信息
- 接口地址：`https://api.deepseek.com/v1/chat/completions`
- 请求方式：`POST`
- 认证方式：`Bearer Token`
- 数据格式：`application/json`

## 2. 请求头
| 字段名 | 值 |
|--------|----|
| Authorization | `Bearer {你的API_KEY}` |
| Content-Type | `application/json` |

## 3. 请求参数
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| model | string | 是 | 模型名，如 `deepseek-chat` |
| messages | array | 是 | 对话消息数组 |
| temperature | number | 否 | 温度系数，0~1，默认 0.7 |
| max_tokens | int | 否 | 最大生成长度 |
| stream | bool | 否 | 是否流式返回，默认 false |

## 4. 响应示例（简化）
```json
{
  "id": "xxx",
  "object": "chat.completion",
  "created": 1234567890,
  "model": "deepseek-chat",
  "choices": [
    {
      "index": 0,
      "message": { "role": "assistant", "content": "返回的内容" }
    }
  ]
}
---

# 搜索功能调研与设计文档（新增）

## 1. 调研背景
校园博客文章数量增多，用户需要通过关键词快速定位内容，因此开发站内搜索功能。

## 2. 需求分析
### 2.1 用户需求
- 支持按文章标题/正文关键词搜索
- 搜索结果带**预计阅读时间**标签
- 关键词高亮显示，方便快速定位
- 无搜索结果时显示友好提示
- 点击结果可直接跳转到真实文章

### 2.2 功能目标
- 搜索响应速度≤1秒
- 结果优先匹配标题，其次匹配正文
- 交互友好：支持回车触发、失焦关闭弹窗

## 3. 竞品参考
参考主流博客站内搜索设计：
- 搜索框固定在页面顶部右侧，不遮挡内容
- 结果以弹窗形式展示，包含标题、摘要和附加信息
- 支持关键词高亮和阅读时间等辅助信息

## 4. 技术方案（Halo博客）
### 4.1 实现思路
- 前端：通过`inject-code.html`注入搜索框、弹窗样式和交互逻辑
- 数据：使用文章列表模拟真实数据，结果跳转`/archives/文章slug`地址
- 附加功能：为文章卡片和搜索结果添加阅读时间标签

### 4.2 核心功能点
1. 顶部右侧固定搜索框（含搜索图标）
2. 回车触发搜索，弹窗展示结果
3. 结果含标题、摘要、灰色阅读时间标签
4. 关键词高亮显示
5. 无结果友好提示：“暂无搜索结果，换个关键词试试吧”
6. 点击标题跳转真实文章

## 5. 验收标准
- ✅ 搜索框正常显示，回车可触发搜索
- ✅ 结果带阅读时间标签，关键词高亮
- ✅ 无结果时显示友好提示
- ✅ 点击标题可跳转真实文章
- ✅ 交互流畅，弹窗可关闭

## 6. 总结
本次调研明确了站内搜索的需求与实现方案，采用前端注入方式快速落地，既满足用户快速找文章的需求，也符合校园博客的整体风格。

# 分类功能完整测试记录

## 一、测试功能概述
本次针对校园博客主题全部分类模块功能进行完整测试，核心包含：分类页顶部介绍文字动态切换、文章卡片彩色分类标签展示、全分类页面适配兼容性测试，确保分类整体功能正常运行。

## 二、已实现功能清单
### 1. 分类页顶部动态介绍文案功能
页面顶部固定展示分类介绍模块，样式统一为主题砖红色配套风格，常驻显示不隐藏。
适配页面刷新、URL跳转场景，通过页面地址自动识别当前分类，无需依赖标签选中状态。
支持6大分类独立专属介绍文案，切换分类自动切换对应文字。
默认首页/全部分类展示通用兜底介绍文案，无空白、无报错。

### 2. 文章卡片彩色分类标签功能
自动识别文章所属分类，将原生纯文字分类替换为彩色圆角小标签。
六大分类独立配色，区分度清晰：默认分类、活动资讯、日常生活、社团动态、学习笔记、校园新闻。
标签样式适配文章卡片布局，悬浮不冲突、排版整齐美观。
页面加载自动渲染，无需手动配置，兼容所有文章卡片样式。

## 三、测试分类及对应文案
默认分类：这里是默认分类，收录未分类的文章内容。
活动资讯：活动资讯 | 校园活动、讲座通知、赛事动态一手掌握。
日常生活：日常生活 | 记录大学生活点滴、学习心得与生活感悟。
社团动态：社团动态 | 各大社团招新、活动分享与成长故事。
学习笔记：学习笔记 | 干货分享、期末复习、考证经验、学习方法。
校园新闻：校园新闻 | 校园大事、通知公告、校园热点全收录。
全部分类（默认页）：这里是分类介绍文字，展示当前分类下的文章内容说明～

## 四、测试场景与结果
| 测试场景 | 测试内容 | 测试结果 |
|--------|----------|----------|
| 分类切换测试 | 依次切换六大分类页面，观察顶部介绍文案是否同步切换 | 全部正常自动切换，文案匹配无误 |
| 页面刷新测试 | 在任意分类页刷新页面，检查介绍文案、彩色标签是否保留 | 功能正常不失效，样式无错乱 |
| 直访URL测试 | 直接通过分类链接地址访问对应分类页 | 可自动识别分类并展示专属文案 |
| 分类标签渲染测试 | 浏览首页及各分类下所有文章卡片 | 彩色标签自动渲染、配色正确、排版整齐 |
| 布局兼容性测试 | PC端、移动端分别查看分类模块 | 自适应布局，无溢出、无遮挡、无错位 |
| 兜底文案测试 | 访问非标准分类、首页全部分类页面 | 正常显示默认兜底文案，无空白无报错 |

## 五、测试结论
校园博客分类模块所有功能均已实现、测试通过，多场景切换、页面刷新、URL直访、移动端适配均稳定正常，样式统一，无报错无错乱，可正式投入使用。